<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Opaline.App.Controls.VideoCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Style="{StaticResource VideoCardStyle}"
            PointerEntered="Root_PointerEntered"
            PointerExited="Root_PointerExited">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Thumbnail -->
            <Grid Grid.Row="0" Height="180" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                <Image x:Name="ThumbImage"
                       Stretch="UniformToFill"
                       DecodePixelWidth="640" />
                <Border HorizontalAlignment="Right" VerticalAlignment="Bottom"
                        Margin="0,0,8,8" Padding="6,2"
                        Background="#CC000000" CornerRadius="4">
                    <TextBlock x:Name="DurationText"
                               Foreground="White"
                               FontSize="12"
                               FontWeight="SemiBold" />
                </Border>
            </Grid>

            <!-- Meta -->
            <StackPanel Grid.Row="1" Padding="12,10,12,12" Spacing="4">
                <TextBlock x:Name="TitleText"
                           TextWrapping="WrapWholeWords"
                           MaxLines="2"
                           TextTrimming="CharacterEllipsis"
                           Style="{StaticResource BodyStrongTextBlockStyle}" />
                <TextBlock x:Name="ChannelText"
                           Opacity="0.7"
                           Style="{StaticResource CaptionTextBlockStyle}" />
                <TextBlock x:Name="ViewsText"
                           Opacity="0.55"
                           Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
