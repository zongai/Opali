# Minimal config for CI unsigned builds
disabled_rules:
  - trailing_whitespace
  - line_length
  - todo
  - force_cast
  - force_try
  - identifier_name
  - type_body_length
  - file_length
  - function_body_length
  - cyclomatic_complexity
  - nesting
excluded:
  - Pods
  - build
  - .build
